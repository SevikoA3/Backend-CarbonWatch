# Example environment variables for Cloud Run or local testing
# If you don't want to use GCS, leave these unset and place files next to app.py

# Local override paths (optional)
# MODEL_PATH=./carbon_watch_model.keras
# PREPROCESSOR_PATH=./preprocessor.pkl
# LABEL_ENCODER_PATH=./label_encoder.pkl

# Option 1: point each artifact directly to a GCS URI
# MODEL_GCS_URI=gs://my-bucket/path/to/carbon_watch_model.keras
# PREPROCESSOR_GCS_URI=gs://my-bucket/path/to/preprocessor.pkl
# LABEL_ENCODER_GCS_URI=gs://my-bucket/path/to/label_encoder.pkl

# Option 2: provide a single prefix and the app will look for standard filenames
# ARTIFACTS_GCS_PREFIX=gs://my-bucket/path/to/artifacts

# For Cloud Run, ensure the service account has "Storage Object Viewer" permission
# and set GOOGLE_APPLICATION_CREDENTIALS if running locally with a service account key file.
# Example (local):
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
